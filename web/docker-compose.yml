services:
  reports-web:
    image: nginx:alpine
    container_name: huawei-reports-web
    restart: unless-stopped
    ports:
      - "18788:80"
    volumes:
      - /home/liangzhu/huawei-reports:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
