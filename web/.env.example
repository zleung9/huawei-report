# HPC head node SSH password for user lz@172.16.12.2
# Used by the nightly cron job (update-slurm.sh) to pull sacct data.
# ⚠️  This is a personal user password — replace with SSH key auth ASAP.
HPC_PASSWORD=changeme
