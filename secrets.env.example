# Copy to secrets.env and fill in. secrets.env is gitignored.
# Used by skill/run.sh -> sourced before invoking expect scripts.

# Password for `ssh hpc` (10.26.15.51, user lz)
HPC_PASSWORD="xxxxxxxx"

# Password for `ssh claw` (10.26.15.53, user liangzhu)
CLAW_PASSWORD="xxxxxxxx"

# Optional: override window size in days (default 30)
# DAYS=60
